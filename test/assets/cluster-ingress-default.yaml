apiVersion: ingress.openshift.io/v1alpha1
kind: ClusterIngress
metadata:
  name: default
spec:
  ingressDomain: apps.CLUSTER_NAME.origin-gce.dev.openshift.com
  routeSelector:
    matchExpressions:
    - {key: environment, operator: NotIn, values: [internal]}
  highAvailability:
    type: Cloud
