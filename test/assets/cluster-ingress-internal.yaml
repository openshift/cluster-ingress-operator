apiVersion: ingress.openshift.io/v1alpha1
kind: ClusterIngress
metadata:
  name: internal
spec:
  ingressDomain: apps.CLUSTER_NAME-internal.origin-gce.dev.openshift.com
  routeSelector:
    matchLabels:
      environment: internal
  highAvailability:
    type: Cloud
